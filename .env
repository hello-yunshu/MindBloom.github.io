# ======================================
# MindBloom 环境变量配置文件
# 注意：本文件只在后端服务器运行时使用，前端无法直接访问
# 生产环境建议使用专门的环境变量管理服务
# ======================================

# --------------------------------------
# MySQL数据库配置
# 注意：请确保已手动创建数据库
# --------------------------------------
DB_HOST=localhost       # 数据库主机地址（默认：localhost）
DB_PORT=3306           # 数据库端口（默认：3306）
DB_USER=root            # 数据库用户名（建议使用非root用户）
DB_PASSWORD=your_mysql_password  # 数据库密码（生产环境请使用强密码）
DB_NAME=mindbloom       # 数据库名称（必须与手动创建的数据库名称一致）

# --------------------------------------
# 服务器配置
# --------------------------------------
PORT=3000               # 服务器端口（默认：3000）
HOST=0.0.0.0            # 服务器监听地址（0.0.0.0 表示允许所有地址访问）

# --------------------------------------
# CORS配置
# 生产环境建议设置为具体域名，例如：https://yourdomain.com
# --------------------------------------
CORS_ORIGIN=*           # 允许的跨域来源（* 表示允许所有来源，生产环境请限制）

# --------------------------------------
# 应用配置
# --------------------------------------
DEFAULT_USERNAME=admin  # 默认用户名（首次登录使用）
DEFAULT_PASSWORD=mindbloom2025  # 默认密码（生产环境请立即修改）

# --------------------------------------
# Qwen API配置（可选）
# 不配置则使用模拟AI建议
# --------------------------------------
QWEN_API_BASE_URL=https://dashscope.aliyuncs.com/api/v1  # Qwen API基础地址
QWEN_API_KEY=your_qwen_api_key  # 阿里云DashScope API密钥（生产环境请妥善保管）
QWEN_MODEL=qwen3-max-preview    # AI模型名称
QWEN_TEMPERATURE=0.7            # 生成文本的随机性（0-1，值越大越随机）

# --------------------------------------
# 定时任务配置
# --------------------------------------
SCHEDULE_TIME=23:00     # 每日定时生成AI内容的时间点（格式：HH:MM）

# --------------------------------------
# 安全提示
# 1. 不要将本文件提交到版本控制系统
# 2. 定期更新数据库密码和API密钥
# 3. 生产环境请使用HTTPS
# 4. 限制CORS来源为具体域名
# 5. 定期备份数据库
# --------------------------------------

